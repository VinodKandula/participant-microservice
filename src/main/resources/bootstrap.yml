spring:
  application:
    name: participant-microservice
  cloud:
    config:
      uri: http://localhost:13301
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 20
